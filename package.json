{
  "name": "vote-me-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "ADD-LIVE-LINK",
  "dependencies": {
    "dotenv": "^16.0.3"
  },
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build"
  },
  
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
